# Contexto operacional — Social API Manager

## Estado atual (resumo vivo)

Última atualização: [2026-09-09]
- Fase: shell React autenticado, dashboard, analytics e gestão de planos conectados. Gateway de pagamento configurado e validado em produção pela primeira vez.
- Estado: o Stripe passou a existir em produção — `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET` e `PAYMENT_GATEWAY` configurados no Railway, e a URL do webhook corrigida. Pagamentos feitos por Payment Link estático passam a ser vinculados à conta do cliente. Antes disso, nenhum checkout jamais havia sido criado em produção.
- Próximo passo: trocar as chaves de teste por chaves live (ver Pendências abertas) e executar um pagamento real de valor baixo.
- Pendências abertas:
  1. **O site não cobra de verdade**: produção está com chaves de *modo teste*; cartão real é recusado. Trocar por `sk_live_` e por um `whsec_` de endpoint criado em modo live.
  2. **Cadastro público bloqueado**: `ALLOWED_EMAIL_DOMAINS=vitissouls.com` faz o cadastro externo responder 403. Decisão do dono do produto; não alterado.
  3. **Modelo de cobrança indefinido**: planos mensais usam `mode=payment` (avulso). Não há renovação, expiração de plano nem tratamento de `invoice.paid`/`customer.subscription.*`.
  4. **Divergência de valor derruba o webhook**: `confirmarPagamento` lança em valor diferente e o webhook responde 500 em reentrega perpétua; deveria virar caso de reconciliação como o `unlinked`.
  5. **Sem ferramenta de reconciliação** para pagamentos marcados `unlinked` (hoje só log).
  6. **`GET /api/billing/plan-link/:plan` sem interface**: quem gera o link para um cliente ainda não foi definido (esbarra na regra de o admin não acessar dados de outra conta).
  7. **Pagamento de ponta a ponta não executado**: os elos foram validados isoladamente, mas ninguém percorreu cadastro → checkout → pagamento → conta ativa.
- Risco aberto: 16 testes de componente falham desde antes desta sessão (`localStorage` indisponível no ambiente do Vitest) — ver Testes importantes [2026-09-09].
- Validação: `npm test` (64 suítes / 584 testes) passou; webhook e criação de checkout validados contra a Stripe real e contra a produção.

## Objetivo do projeto

Gerenciar publicações e métricas de redes sociais em uma única interface, com autenticação e integrações oficiais de cada plataforma.

## Regras desejadas para a IA

- Entender pedidos naturais, abreviações e erros de digitação sem exigir que o usuário conheça o nome interno da função.
- Antes de afirmar que concluiu uma ação, confirmar que o backend devolveu um resultado válido; nunca simular publicação, imagem, métrica ou conta.
- Ao criar conteúdo, adaptar texto, título, hashtags, tom e tamanho para cada rede escolhida e sempre recomendar revisão antes de publicar.
- Ao criar uma imagem, preservar a descrição visual, tentar o modelo preferido e trocar automaticamente para outro provedor configurado se houver erro, quota, billing ou resposta sem imagem.
- Oferecer ao usuário um seletor de modelos (Assistente Rápido, Gemini, GPT, OpenRouter e Claude), salvar a preferência e mostrar qual modelo foi utilizado; manter o fallback automático para imagens.
- Validar a saída da geração de imagem antes de mostrá-la. Só informar sucesso quando houver uma URL ou data URL de imagem válida e informar qual modelo acabou sendo usado.
- Se todos os provedores falharem, explicar o problema de forma curta, registrar o diagnóstico e oferecer a ação mais útil (tentar novamente, configurar uma chave ou abrir o módulo apropriado).
- Não publicar, excluir, desconectar, responder comentários ou alterar dados sem confirmação explícita quando a ação for destrutiva ou externa.
- Em produção, nunca declarar uma alteração como concluída sem verificar o deploy, o endpoint público, os logs/status da aplicação, os testes automatizados e, quando houver integração externa, uma chamada real de conexão com o provedor.
- Não inventar dados de analytics: separar claramente dados reais da conta, estimativas e recomendações gerais.
- Fazer uma pergunta por vez quando faltar informação essencial e aproveitar a resposta seguinte para completar o plano pendente.
- Em pedidos compostos, executar uma única ação por vez e explicar a próxima etapa, evitando alterações escondidas.

## Stack & dependências

- [2026-07-31] Backend: Node.js + Express.
- [2026-07-31] Frontend: React 19 + Vite, sem biblioteca adicional de UI.
- [2026-07-31] CSS: tokens e componentes próprios em `frontend/src/styles/tokens.css`.

## Decisões de arquitetura

- [2026-07-31] O novo frontend fica em `frontend/` e gera artefatos em `public/react/`, mantendo o backend Express e as rotas existentes.
- [2026-07-31] A primeira fatia migrada é a landing page pública; o dashboard legado permanece funcional para reduzir risco e permitir migração incremental.
- [2026-07-31] Componentes seguem a separação `components/ui`, `components/layout`, `sections` e `pages`, com arquivos `kebab-case.jsx`.
- [2026-08-03] Regras de validação client-side que espelham `src/domain/posts/*.js` ficam em `frontend/src/lib/*.js` (funções puras, sem React) — ex.: `postValidation.js`. O comentário de topo do arquivo aponta para o(s) arquivo(s) de domínio correspondente(s) no backend, que continuam sendo a fonte da verdade.
- [2026-08-24] CONTEXTO: o requisito passou a permitir múltiplas contas por rede, inclusive para usuários de um domínio corporativo autorizado, e a pessoa precisa escolher quais contas recebem cada publicação. DECISÃO: manter a relação N:M já existente em `post_accounts`, exibir todas as contas conectadas por plataforma no agendador React com seleção individual e enviar `accountIds` explícitos ao criar o post; quando omitido, o backend preserva o comportamento legado de usar todas as contas ativas. A validação server-side rejeita seleção vazia, IDs inválidos/duplicados e contas de redes não escolhidas. O aplicativo não adiciona limite de quantidade; limites comerciais do provedor externo continuam fora do controle do código. VALIDAÇÃO: `npm test` (43 suítes / 437 testes), `npm run test:components` (13 arquivos / 63 testes), `npm run frontend:build` e `git diff --check`. Teste manual autenticado não executado neste ambiente sem navegador/DB.
- [2026-08-24] CAUSA RAIZ: a tela branca após o retorno OAuth ocorria porque `/auth/*/zernio-return` entregava JavaScript inline dinâmico, bloqueado pela CSP de produção (`script-src` permitia apenas hashes fixos); por isso o redirecionamento e `window.close()` nunca executavam. DECISÃO: mover o comportamento para `public/oauth-popup.js`, servido same-origin e permitido por `script-src 'self'`, com mensagem visual e botão de fechamento como fallback. VALIDAÇÃO: suíte OAuth (7 testes), `npm test` (43 suítes / 439 testes), `node --check`, `git diff --check`, endpoint público do callback e asset 200 após deploy Railway `7584fa01-08d4-44da-ac99-4f1c74e6224d`.
- [2026-08-24] AGENDADOR: contas conectadas agora são agrupadas por pessoa usando `ownerEmail`/`userId`, reunindo todas as redes do mesmo dono no painel “Contas por pessoa” e mantendo pessoas diferentes separadas. Cada conta exibe rede, handle/nome, avatar e status do token; a seleção continua individual e só permite marcar contas cuja rede foi selecionada. VALIDAÇÃO: `npm run test:components` (13 arquivos / 63 testes), `npm run frontend:build` e `git diff --check`.
- [2026-08-24] CORREÇÃO: a prévia de imagens locais no Agendador usava URLs `blob:` e era bloqueada pela CSP do Railway, que liberava `blob:` apenas em `media-src`. DECISÃO: incluir `blob:` em `img-src`, mantendo as demais restrições. VALIDAÇÃO: teste de CSP atualizado, suíte backend e verificação do cabeçalho em produção após deploy.
- [2026-08-24] SAÚDE DAS PLATAFORMAS: o teste real mostrou Zernio HTTP 200, mas contas antigas retornando `Account not found`; esse erro de conexão estava sendo confundido com indisponibilidade da API. DECISÃO: a sonda passa a testar `/accounts` do Zernio, e o mapa do painel filtra a existência de conta por `user_id`, evitando exibir falha de outro usuário ou de uma rede que a pessoa não conectou.
- [2026-08-24] PLANOS: criado o tier pago 1 `basico` (EcooMidia Básico) por R$ 52,50/mês, com o checkout Stripe informado pelo usuário. O catálogo inclui análise de métricas (`analytics`) e os módulos correspondentes às funcionalidades anunciadas, mantendo os planos existentes.
- [2026-08-24] ACESSO E PLANOS: o plano Gratuito foi removido do catálogo. `basico` virou o plano padrão, registros antigos com `plan='gratuito'` são migrados para `basico`, e o cadastro usa o checkout configurado do tier escolhido. O acesso à aplicação ficou restrito à allowlist de domínio configurada em ambiente, inclusive login, recuperação de senha, Google OAuth e sessões existentes.

- [2026-08-28] CAUSA RAIZ/AGENDADOR: a descrição do TikTok era editada em `textByPlatform.tiktokDescription`, mas `src/infra/social/publisher.js` resolvia somente `textByPlatform.tiktok` ou `post.text`. Como o agendador React não envia `text` principal, o publisher repassava texto vazio ao adapter e a descrição não chegava ao Zernio. DECISÃO: resolver explicitamente `tiktokDescription` e manter `tiktok` como compatibilidade legada; o adapter continua enviando a descrição longa em `tiktokSettings.description` para carrosséis e em `content` para vídeos, conforme o contrato do Zernio.
- [2026-08-28] MÍDIA POR REDE: o agendador passou a aceitar mídia compartilhada ou uma substituição própria em cada cartão de rede, com identificação de imagem/vídeo e carrossel de fotos. A mídia própria é enviada em `mediaByPlatform`, aproveitando a migration `035_post_accounts_media.sql` e o pipeline já existente de normalização/conversão do backend. Regras aplicadas no editor: TikTok aceita um vídeo ou até 35 fotos; Instagram e Facebook aceitam um vídeo ou até 10 fotos; YouTube aceita um vídeo. Vídeos e fotos não são misturados no mesmo destino.
- [2026-08-28] VALIDAÇÃO: testes focados da descrição/publicador passaram (6 testes), validação de mídia passou (19 testes), backend completo passou (55 suítes/490 testes), frontend completo passou (15 arquivos/73 testes), `npm run frontend:build` passou (118 módulos) e `git diff --check` passou. Deploy e publicação real no TikTok não foram executados nesta sessão.
- [2026-08-28] DEPLOY: correção da descrição do TikTok e mídia por rede publicada no GitHub (`eafe400`), Railway (`738b5890-dbd1-40ed-8982-ac5878bc5705`, SUCCESS) e Vercel (`dpl_FJC58hiS4SRypWNT6sZpEjLLnpLd`, READY), com alias `https://meuecoomidia.com.br`. Verificação externa: `/health` e `/api/config` no Railway e `/app/agendador`/`/api/config` no domínio responderam HTTP 200.
- [2026-08-28] CORREÇÃO ADICIONAL/TIKTOK: a descrição ainda era descartada no frontend ao montar `platformTexts`, porque o filtro comparava a chave `tiktokDescription` diretamente com a plataforma `tiktok`. DECISÃO: centralizar `textsForSelectedPlatforms()` para preservar a chave específica do TikTok no POST. VALIDAÇÃO: teste de regressão do payload passou e o bundle foi reconstruído.

- [2026-09-07] CORREÇÃO/UX: a sidebar desenhava uma barra de rolagem mesmo quando seu conteúdo cabia no viewport porque `.app-sidebar` usava `overflow-y: scroll`. DECISÃO: usar `overflow-y: auto`, preservando o gutter estável e os estilos de scrollbar existentes; a barra continua disponível em alturas realmente insuficientes sem criar um trilho persistente no shell. VALIDAÇÃO: Jest (61 suítes/533 testes), Vitest de componentes (24 arquivos/110 testes), teste focado do `AppShell` (9 testes), build frontend (128 módulos) e `git diff --check` passaram antes e depois da alteração.

## Decisões de design & convenções

- [2026-07-31] Aplicado o padrão Felixo de composição, mobile-first, foco visível, contraste, estados de interação e espaçamento consistente, adaptando a paleta dourada já usada pelo produto.
- [2026-08-07] O item Assistente IA da navegação lateral passou a usar um ícone de robô em SVG linear, com o mesmo traço e comportamento visual dos símbolos dos demais itens. A estrutura da navegação e a interação permaneceram inalteradas.
- [2026-08-07] No agendador, removido o campo visual de texto padrão e reposicionados os campos de texto específicos por rede antes do assistente de conteúdo. A geração de descrição continua enviando as redes selecionadas à IA e agora aplica cada sugestão ao campo correspondente, usando os textos atuais por rede como contexto de melhoria.
- [2026-08-07] Correção do agendador: removida também a dependência interna do texto padrão. Cada rede agora é editada, validada, salva em rascunho/modelo, exibida no preview e enviada à API exclusivamente pelo seu campo específico; drafts antigos com texto padrão são migrados para as redes selecionadas ao reabrir.
- [2026-08-07] O calendário passou a exibir prévia de imagem/vídeo e o texto específico da rede no detalhe/lista. Falhas de mídia ou publicação mostram uma mensagem amigável; a API passou a carregar `errorMessage` e persistir a causa técnica para traduzir casos de conteúdo removido/indisponível.
- [2026-08-07] A tela de Tokens foi reorganizada como uma área de segurança: cabeçalho com estado geral, indicadores de saúde, alerta de pendências, filtros rotulados e cards responsivos por integração. As ações e contratos de API de renovar/revogar foram preservados.
- [2026-08-07] A seleção de cards do Inbox foi refinada com checkbox customizado, estado visual selecionado e grade desktop corrigida para separar checkbox, miniatura, rede, texto e ações sem sobreposição. Também foi criado um padrão global de scrollbar fino, dourado e acessível para listas e painéis com rolagem.
- [2026-07-31] Commits futuros seguem Conventional Commits (`feat:`, `fix:`, `docs:`, `refactor:`, `chore:`).

- [2026-08-07] CONTEXTO: o pedido passou a exigir visualizações por publicação e identificação agregada do público por gênero/faixa etária. DECISÃO: manter a coleta real já existente, ampliar a normalização de aliases (`views`, `viewCount`, `video_views`, `media_views`, `plays` etc.) e tornar a visualização por post sempre explícita, mostrando “—” quando a rede não devolve o número. Na demografia, separar gênero e faixa etária, localizar Homem/Mulher, identificar a fonte e explicar que são grupos agregados de seguidores ou espectadores — não pessoas individualmente identificáveis. Instagram usa seguidores/audiência engajada quando a API oficial/Zernio devolve; YouTube usa espectadores; Facebook/TikTok exibem a limitação oficial. VALIDAÇÃO: `npm test` (31 suítes / 353 testes), `npm run frontend:build` (91 módulos) e `git diff --check`. Estado: CONCLUÍDO. — Especialista em UI e UX
- [2026-08-07] Deploy de produção concluído por Especialista em UI e UX: Vercel `dpl_7sswCrHDkSD71mnP5UsT9vGK3QU9`, alias `https://meuecoomidia.com.br`, status Ready; Railway `f5749a31-2b68-47b7-8e20-a011e19a7aae`, status Online. Verificação: frontend `/app/analytics` respondeu HTTP 200 e API `/api/config` respondeu HTTP 200.
- [2026-08-07] DIAGNÓSTICO: o botão “Ver relatório” do Analytics está conectado ao handler React, mas o painel não aparece porque a resposta real de produção retorna `accountAnalytics.platforms.facebook/instagram/tiktok` vazios. A causa está em `listarContasToken()` (`src/infra/social/publisher.js`): a consulta inclui `c.zernio_account_id`, mas não seleciona `t.platform`; `accountAnalyticsService.collectAccount()` recebe `token.platform` como `undefined`, falha na condição `ZERNIO_PLATFORMS.includes(token.platform)` e devolve apenas erro de provider. Como `AnalyticsAccountInsights` retorna `null` quando não encontra contas, o `scrollIntoView()` também não encontra `#analytics-account-report`. O endpoint confirmado em produção foi `GET /api/posts/analytics?days=7`, com plataformas vazias. Nenhum código foi alterado nesta investigação. — Especialista em UI e UX
- [2026-08-07] CORREÇÃO/DEPLOY: adicionada a coluna `t.platform` ao `SELECT` de `listarContasToken()` em `src/infra/social/publisher.js`. A API passou a retornar 1 conta Instagram e 1 TikTok em `accountAnalytics.platforms`; Vercel `dpl_Ewg4MQP2rMic8zSNeGMdMYezmiaY`, alias `https://meuecoomidia.com.br`, Ready; Railway `fd231e47-801d-41b8-b6aa-42637e6978b5`, Online. Healthchecks: frontend e API HTTP 200. — Especialista em UI e UX
- [2026-08-07] UX/analytics: o relatório completo do perfil estava visualmente comprido porque empilhava gráfico de evolução, tabela diária completa e gráfico de dimensões abertos, sem viewport fixo para os canvases. DECISÃO: limitar o gráfico principal a até 30 pontos distribuídos no período, limitar os ticks do eixo X, fixar a área visual em 250px (220px no mobile) e mover a série diária completa/detalhes por dimensão para accordions recolhidos, mantendo todos os dados disponíveis. VALIDAÇÃO: `npm run frontend:build`, `npm run test:components` (10 arquivos / 40 testes) e `git diff --check`. Estado: CONCLUÍDO. — Especialista em UI e UX
- [2026-08-07] DEPLOY: melhoria do relatório completo do perfil publicada em produção por Especialista em UI e UX via Vercel `dpl_9gLvgfeuWwpdT1jJ2cc9khLinTim`, alias `https://meuecoomidia.com.br`, status Ready. Verificação: `/app/analytics` respondeu HTTP 200.
- [2026-08-07] DIAGNÓSTICO/CALENDÁRIO: um post criado para duas redes não está sendo perdido na API. `listarPostsCalendario()` seleciona o array completo `platforms` da tabela `posts`, e o formulário salva as duas plataformas. A exibição do detalhe em `frontend/src/pages/calendar-page.jsx` usa `const primaryPlatform = platformsOf(post)[0]` e renderiza apenas esse ícone/texto; por isso o Instagram aparece como primeira rede e a segunda fica invisível nessa visão. O modo Lista já percorre `platformsOf(post)` e mostra todas. Nenhum código alterado nesta investigação. — Especialista em UI e UX
- [2026-08-07] CORREÇÃO/DEPLOY CALENDÁRIO: `frontend/src/pages/calendar-page.jsx` passou a renderizar badges de todas as plataformas no evento do dia e no detalhe/modal, e `frontend/src/styles/calendar.css` recebeu o estilo dos badges por rede. A API foi preservada porque já retornava `platforms` completo. VALIDAÇÃO: `npm run frontend:build`, `npm run test:components` (10 arquivos / 40 testes) e `git diff --check`. Publicado por Especialista em UI e UX via Vercel `dpl_DaHTXsdLxGPc85qEeUgXtcD7Mikn`, alias `https://meuecoomidia.com.br`, status Ready; `/app/calendario` respondeu HTTP 200.

## Testes importantes

- [2026-09-09] ✅ `npm test` (64 suítes / 584 testes) — inclui `tests/integration/billingWebhook.test.js`, que exercita o webhook no nível HTTP assinando o payload com HMAC real: assinatura inválida, segredo errado, replay fora da janela de tolerância, corpo não-JSON, os dois caminhos de pagamento, liquidação assíncrona (pix/boleto), idempotência de reentrega e os casos sem vínculo. Também `tests/unit/repositories/billingRepository.test.js`, cobrindo a transação do vínculo direto (commit, rollback, idempotência, conflito de cobrança no mês).
- [2026-09-09] ✅ Qualidade dos testes verificada por mutação, não só pelo verde: alterar o padrão do `client_reference_id`, remover a resolução por e-mail e inverter a ativação do plano derrubaram exatamente os testes correspondentes.
- [2026-09-09] ✅ Validação ao vivo contra a API da Stripe (modo teste), usando o próprio `stripeGateway`: a sessão criada retorna `client_reference_id`, `metadata.to_plan` e `amount_total` coerentes com o que o app grava; `expireCheckout` também confirmado. Sessão de teste expirada em seguida.
- [2026-09-09] ✅ Validação do webhook contra produção com evento assinado: assinatura válida → HTTP 200; segredo forjado → HTTP 400; timestamp fora da janela → HTTP 400. Sonda escolhida sem efeito colateral (`payment_status: unpaid` retorna antes de tocar no banco).
- [2026-09-09] ❌ `npm run test:components` — 3 arquivos / 16 testes falhando (`app-shell`, `dashboard-page`, `calendar-page`) com `TypeError: localStorage.getItem is not a function`. Falha **pré-existente**, confirmada rodando a suíte no commit anterior a este trabalho: os componentes passaram a ler `localStorage` na inicialização de estado e o ambiente do Vitest não o fornece. Nenhum arquivo de frontend foi alterado nesta sessão.
- [2026-08-04] ✅ `npm run test:components` (7 arquivos / 32 testes) e ✅ `npm run frontend:build` — passaram após adicionar o acompanhamento do resultado assíncrono de publicação e os testes do cursor/erro em `publication-events.test.jsx`.

- [2026-07-31] ✅ `npm run frontend:build` — bundle React produzido em `public/react/`.
- [2026-07-31] ✅ `npm test -- --runInBand` — 21 suítes e 305 testes passaram após impedir o bypass de revisão em `NODE_ENV=test`, desabilitar cache de usuário nos testes e alinhar o issuer TOTP padrão ao contrato existente.
- [2026-08-03] ✅ `npm run frontend:build` e ✅ `npm test` (21 suítes / 305 testes) — passaram após adicionar `frontend/src/lib/postValidation.js` e as mudanças em `scheduler-page.jsx` (nenhum teste de backend foi afetado; não há suíte de frontend configurada no repositório — `jest.testMatch` só cobre `tests/**/*.test.js` no backend — então a lib nova ficou sem teste automatizado dedicado, coberta só pelo build/typecheck do Vite).
- [2026-08-03] ⚠️ Não executado: teste manual autenticado no navegador (login real + agendar um post por plataforma). Este ambiente de execução não tem navegador nem acesso ao Postgres configurado em `DATABASE_URL` — só shell. Fica como pendência explícita para quem tiver acesso a um ambiente com navegador e banco.

## Resumos de decisão

- [2026-08-05] CONTEXTO: a tela de analytics mostrava números corretos, mas usava rótulos ambíguos como “Alcance”, “Views” e “Watch Time” e não explicava como comparar as redes. ALTERNATIVAS: alterar o contrato da API, criar uma nova dependência de gráficos ou melhorar a camada React existente. DECISÃO: manter a API intacta e reorganizar a experiência em resumo do período, painel da rede e lista de conteúdos; trocar os rótulos por linguagem comum, adicionar explicações curtas, estado vazio orientativo, controles acessíveis e filtrar vídeos do TikTok pelo mesmo período das demais métricas. Também foi incluído o compartilhamento na série de interações. VALIDAÇÃO: `npm run test:components` (10 arquivos / 36 testes), `npm run frontend:build` e `git diff --check` passaram. Estado: CONCLUÍDO. As alterações backend e de dados não relacionadas que já estavam no workspace foram preservadas para o agente responsável.

- [2026-08-04] CONTEXTO: testes de POST feitos em outro computador não exibiam falhas de publicação no frontend React. CAUSA: `POST /api/posts` retorna `202 processing` para `publishNow` e conclui a integração social em segundo plano; `scheduler-page.jsx` ignorava o corpo da resposta, limpava o formulário e mostrava sucesso, mas não consumia o evento `post_published` já emitido pelo backend. DECISÃO: reutilizar o polling existente de `/api/logs/events/since/:lastId`; capturar/drenar o cursor antes do POST para não reexibir histórico antigo e acompanhar apenas o ID retornado, apresentando os erros de `results` por plataforma/conta com `role="alert"`. A alteração manual foi escolhida porque não há script de transformação para componentes React e o ajuste é localizado. VALIDAÇÃO: `npm run test:components` (7 arquivos / 32 testes), `npm run frontend:build` e `git diff --check` passaram. Estado: CONCLUÍDO.
- [2026-08-05] CONTEXTO: o analytics anterior cobria apenas parte das métricas agregadas e alguns snapshots locais. DECISÃO: centralizar o contrato em `src/domain/analytics/analyticsCatalog.js`, normalizar respostas heterogêneas em `normalizeAnalytics.js`, consultar os endpoints oficiais disponíveis do Zernio para Facebook/Instagram/TikTok e a YouTube Analytics API para relatórios diários, demografia, fontes, reprodução, inscrição, tipos de conteúdo e receita; preservar o legado e acrescentar `accountAnalytics` na rota `GET /api/posts/analytics?days=1..90`. O frontend exibe métricas, séries, dimensões, métricas diárias, crescimento, decadência e limitações não expostas. Falhas são isoladas por relatório/rede e não interrompem o restante. Estado: CONCLUÍDO.
- [2026-08-05] COMPLEMENTO: conectado também o relatório oficial de melhores horários para publicar e o formato `dailyData` das métricas agregadas; o frontend mostra os cinco melhores slots por rede. Estado: CONCLUÍDO.
- [2026-08-05] CONTEXTO: o chat legado interpretava “analytics das redes sociais conectadas” como início do fluxo de criação de posts porque usava Instagram como padrão quando não encontrava uma plataforma. DECISÃO: encaminhar intenções operacionais ao `/api/ai/agent` antes do roteiro de posts, manter fallback para `/api/posts/analytics?days=90` e dar prioridade à geração apenas quando houver intenção explícita de conteúdo; a mesma regra foi corrigida no intérprete backend. VALIDAÇÃO: 25 suítes/322 testes, checagem de sintaxe do HTML legado e `git diff --check` passaram. Estado: CONCLUÍDO.
- [2026-08-05] CONTEXTO: a auditoria para tornar o agente capaz de atender pedidos naturais em toda a aplicação identificou que o catálogo atual cobre apenas uma parte dos módulos. Foram mapeadas como próximas capacidades: dashboard, detalhes/histórico de posts, reagendamento/cancelamento, inbox não lido e marcar como visto, TikTok, textos salvos, presets, saúde das plataformas, logs, memórias e preenchimento conversacional de campos ausentes. DECISÃO: ampliar o catálogo único, manter regras de leitura/escrita separadas, exigir confirmação para alterações destrutivas/publicações e reutilizar os contratos reais existentes. Estado: INTERROMPIDO POR SOLICITAÇÃO DO USUÁRIO; nenhum código dessa expansão foi alterado ainda.

- [2026-07-31] CONTEXTO: introduzir React puro e modularização sem interromper o produto existente. ALTERNATIVAS: reescrever o dashboard inteiro ou migrar uma fatia vertical. DECISÃO: migrar a landing pública primeiro, isolando o build e conectando apenas `/` e `/sobre`. VALIDAÇÃO: build Vite e verificação das rotas Express.
- [2026-07-31] CONTEXTO: conectar o dashboard ao React sem descartar comportamentos legados. ALTERNATIVAS: apagar o `app.html` ou migrar por fatias. DECISÃO: conectar `/app.html` ao shell React, manter o legado no repositório e migrar cada módulo com seus contratos de API. VALIDAÇÃO: build Vite passou; a migração integral fica interrompida nesta etapa para evitar regressão silenciosa.
- [2026-07-31] CONTEXTO: avançar a migração funcional. DECISÃO: migrar rascunhos (listar, salvar e excluir) e contas conectadas (listar e desconectar) em componentes próprios, reutilizando `apiFetch`. VALIDAÇÃO: build Vite passou; os demais módulos permanecem preservados para a próxima fatia.
- [2026-07-31] CONTEXTO: substituir telas-base de calendário e analytics. DECISÃO: consumir `/api/posts/calendar` e `/api/posts/analytics` diretamente em componentes React, com navegação mensal e métricas numéricas sem nova dependência. VALIDAÇÃO: build Vite passou.
- [2026-07-31] CONTEXTO: substituir telas-base de inbox, tokens e IA. DECISÃO: consumir `/api/posts/inbox`, `/api/tokens` e `/api/ai/generate` com ações de renovação, remoção e geração de sugestões. VALIDAÇÃO: build Vite passou.
- [2026-07-31] CONTEXTO: migrar criação e agendamento. DECISÃO: iniciar com texto, data/hora e plataformas usando o contrato JSON de `/api/posts`; upload e configurações avançadas ficam isolados para a próxima fatia. VALIDAÇÃO: build Vite passou.
- [2026-07-31] CONTEXTO: adicionar mídia ao agendamento. DECISÃO: solicitar URL assinada em `/api/posts/upload-url`, enviar o arquivo diretamente por `PUT` e enviar metadados em `media` ao criar o post. VALIDAÇÃO: build Vite passou.
- [2026-07-31] CONTEXTO: completar as opções essenciais do agendador. DECISÃO: enviar `youtubeTitle`, `youtubeVisibility`, `igFormat`, `tiktokPrivacyLevel`, `firstComment` e `textByPlatform` pelos campos já suportados em `/api/posts`. VALIDAÇÃO: build Vite passou.
- [2026-07-31] CONTEXTO: concluir ações do calendário. DECISÃO: usar `PATCH /api/posts/:id` para reagendar e `DELETE /api/posts/:id` para excluir, com confirmação antes da exclusão. VALIDAÇÃO: build Vite passou.
- [2026-07-31] CONTEXTO: suportar publicação imediata no agendador. DECISÃO: alternar `publishNow` e usar o horário atual quando o modo imediato estiver ativo, mantendo data/hora obrigatória apenas para agendamento. VALIDAÇÃO: build Vite passou.
- [2026-08-03] CONTEXTO: fechar as validações específicas de cada plataforma no agendador React (próximo passo registrado em 2026-07-31), usando `src/domain/posts/post.js` (`validarCriacaoPost`) e `src/domain/posts/videoRules.js` (`isAspectRatioValidForTiktok`) como fonte da verdade — o app legado (`public/app.html`) foi auditado antes e tem duas lacunas reais que ele mesmo nunca cobriu no cliente: Instagram/TikTok exigem mídia mas isso nunca era checado antes do submit, e a proporção 9:16–16:9 do TikTok só era descoberta depois do upload, no erro 400 do backend. ALTERNATIVAS: (a) copiar 1:1 a lógica do `app.html` (`buildValidationIssues`/`schedulePost`) — descartada por herdar essas lacunas; (b) confiar só na mensagem de erro do backend após o submit — descartada por ser pior experiência e não seguir o padrão de qualidade (validar entrada antes de gastar upload). DECISÃO: criar `frontend/src/lib/postValidation.js` (funções puras, sem I/O exceto `readVideoMeta`, que lê metadados do vídeo no navegador via elemento `<video>` para checar a proporção do TikTok antes do upload) e usá-lo em `scheduler-page.jsx` para (1) um painel de pendências não-bloqueante (`.validation-panel`, estilo em `modules.css`) que atualiza a cada mudança do formulário, e (2) bloquear o submit (botão desabilitado + mensagem) enquanto houver pendência. Também corrigidos dois bugs pré-existentes descobertos durante a auditoria: `igFormat` tinha valor default/opção `"feed"`, mas o backend só aceita `post`/`reel`/`story` (todo post do Instagram com formato Feed falhava a validação); e o `<textarea>` do texto tinha `required`, impedindo posts só-com-mídia (o backend aceita texto OU mídia). Adicionado também o campo obrigatório `youtubeMadeForKids` (select Sim/Não), que nunca existia no formulário React — sem ele, todo post com YouTube selecionado era rejeitado pelo backend (COPPA/FTC, `post.js:139-143`). Campos opcionais por rede que o backend já suporta mas o formulário React ainda não expõe (categoria/formato do YouTube, local do Facebook/Instagram, `threadsReplyControl`, `linkedinVisibility`, board do Pinterest, `tiktokDisableComment/Duet/Stitch`) ficaram de fora desta fatia — não são "validação", são campos novos, registrados como próximo passo. VALIDAÇÃO: `npm run frontend:build` e `npm test` (21 suítes / 305 testes) passaram; teste manual autenticado em navegador não foi executado (sem navegador/DB neste ambiente — ver Testes importantes). RISCO: as regras client-side duplicam constantes do backend (ex.: `INSTAGRAM_MIN_ANTECEDENCIA_MIN = 20`, faixa de proporção do TikTok) — se alguém mudar `post.js`/`videoRules.js` sem atualizar `postValidation.js`, o aviso no React fica desatualizado (o backend continua sendo quem bloqueia de verdade, então não há risco de dado inválido entrar, só de o aviso ficar impreciso).
- [2026-08-03] CONTEXTO: expor no agendador React os campos opcionais por rede ainda ausentes (próximo passo registrado acima). Antes de implementar, `src/utils/http.js` (`PLATFORMS`) foi conferido e confirma que a integração real só cobre `facebook`, `instagram`, `youtube`, `tiktok` — Threads/LinkedIn/Pinterest citados na decisão anterior não têm nenhuma rota/publisher no backend (não é dívida técnica do frontend, é um passo à frente do que existe; a menção anterior estava incorreta e foi corrigida no resumo de estado). Os campos realmente pendentes eram: `youtubeCategoryId`, `youtubeFormat`, `tiktokDisableComment/Duet/Stitch` e o local do Facebook/Instagram (`locationId`/`locationName`, com busca via `GET /api/posts/facebook-places?q=`). DECISÃO: adicionar em `scheduler-page.jsx` — (1) select de categoria do YouTube com a mesma lista de `YOUTUBE_CATEGORIES` (`post.js`), duplicada em `scheduler-page.jsx` com comentário apontando a fonte, mesmo padrão já usado em `postValidation.js`; (2) select de formato do YouTube (Automático/Vídeo/Short); (3) três checkboxes de interação do TikTok, desmarcadas por padrão = permitido, espelhando a leitura de `body.tiktokDisableComment` em `criarPost.js`; (4) campo de busca de local com debounce de 400ms (>=3 caracteres), replicando o comportamento de `onLocationSearchInput` do `app.html`, consumindo `apiFetch('/api/posts/facebook-places?q=...')` e listando resultados clicáveis com opção de remover a seleção. ALTERNATIVAS: adicionar campos para Threads/LinkedIn/Pinterest também — descartada porque não haveria endpoint/publisher no backend para consumir esses valores, só criaria UI morta. VALIDAÇÃO: `npm run frontend:build` e `npm test` (21 suítes / 305 testes) passaram; teste manual autenticado em navegador não executado (mesma limitação de ambiente já registrada). Estado: CONCLUÍDO — nenhum campo opcional das 4 plataformas suportadas ficou de fora do formulário React.
- [2026-08-04] CONTEXTO: a validação de criação em `src/domain/posts/post.js` repetia quatro vezes a mesma decisão de limite de texto/título dentro de uma função já extensa. DECISÃO: extrair `validarLimiteDeTexto` e `validarLimitesDeTextos`, funções puras e reutilizáveis, preservando mensagens, ordem das validações e contrato de `validarCriacaoPost`. A edição foi manual porque não havia automação de refatoração configurada para o domínio; o recorte foi localizado para evitar interferência nas alterações paralelas. VALIDAÇÃO: testes unitários do domínio e suíte completa passaram. Estado: CONCLUÍDO.
- [2026-08-04] CONTEXTO: a auditoria de código legado confirmou que o dashboard e seus módulos já estavam migrados para React, mas o fallback `app.get('*')` ainda servia `public/app.html` para URLs de interface não reconhecidas. DECISÃO: direcionar o fallback para `public/react/index.html`, garantindo que toda rota de interface use o shell React; manter `public/app.html` apenas como referência histórica durante a remoção futura das referências documentais. As páginas de autenticação e legais continuam estáticas por serem superfícies independentes do dashboard. VALIDAÇÃO: build frontend e suíte backend executados. Estado: CONCLUÍDO.
- [2026-08-05] CONTEXTO: o widget do Agente IA enviava qualquer texto para `/api/ai/generate` como se fosse um pedido de legenda para Instagram, sem compreender os módulos nem executar funções da aplicação. ALTERNATIVAS: manter respostas livres sem execução, duplicar cada tela no frontend ou criar um catálogo único com executor backend. DECISÃO: criar `src/services/ai/agentCatalog.js`, `agentInterpreter.js` e `agentExecutor.js`; o catálogo registra as capacidades reais, regras locais atendem pedidos comuns sem custo de IA, e o modelo só interpreta pedidos não reconhecidos em JSON estrito. Consultas usam os casos de uso/repos existentes; gravação de rascunho, exclusão, desconexão, renovação e resposta pública exigem token assinado de confirmação com validade de cinco minutos. O widget React envia `/api/ai/agent`, mostra dados estruturados e navega para o módulo retornado. OAuth, upload e publicação avançada são encaminhados ao módulo porque exigem interação explícita e mídia/credenciais que o agente não deve inventar. VALIDAÇÃO: `npm run test:components` (7 arquivos / 32 testes), `npm run frontend:build`, `npm test` (23 suítes / 311 testes), `node --check` nos novos módulos e `git diff --check`. Estado: CONCLUÍDO.
- [2026-08-05] CONTEXTO: a expansão pausada precisava cobrir as funções operacionais já disponíveis na aplicação. DECISÃO: ampliar o catálogo/intérprete/executor com resumo do dashboard, detalhes e histórico de métricas de posts, reagendamento e cancelamento com confirmação, inbox não lido e marcar comentários como vistos, vídeos/opções do TikTok, textos salvos, presets, saúde das plataformas, logs e memórias da IA. As leituras reutilizam casos de uso e repositórios existentes; as alterações continuam protegidas por confirmação e escopo do usuário. O intérprete passou a reconhecer datas relativas com horário e prioriza intenções específicas antes de aliases de navegação. O widget passou a resumir os novos formatos de dados. VALIDAÇÃO: `npm test` (25 suítes / 325 testes), `npm run test:components` (10 arquivos / 36 testes), `npm run frontend:build`, `node --check` e `git diff --check`. Estado: CONCLUÍDO.
- [2026-08-17] CONTEXTO: o usuário precisava escolher um plano e pagar uma única vez, sem cobrança duplicada no mesmo mês e sem cobrança recorrente automática. DECISÃO: usar Checkout hospedado do Stripe em `mode=payment`; remover cartão/Pix do frontend; iniciar qualquer conta paga no Gratuito; criar `billing_plan_changes` com unicidade `(user_id, billing_month)` e chave idempotente estável; reservar o processamento antes da chamada externa; ativar o plano somente no webhook assinado após conferir sessão, plano, valor e moeda; tratar resposta incerta como `processing`; cancelar/expirar checkout aberto quando a pessoa escolher o Gratuito e ignorar webhooks de tentativas canceladas/falhas. Rotas: `GET /api/billing/status`, `POST /api/billing/plan-change` e `POST /api/billing/stripe/webhook`. Documentação: `README.md` e variáveis Stripe no `.env.example`. Validação: 8 testes específicos de billing, `npm test` (37 suítes / 412 testes), `npm run test:components` (12 arquivos / 60 testes), `npm run frontend:build`, `node --check` e `git diff --check`. Estado: CONCLUÍDO; aguarda somente configuração e teste do gateway em homologação. — Desenvolvedor com verificação para gateway para pagamento.
- [2026-08-17] DEPLOY: backend publicado no Railway em produção no deployment `57b374d6-f0c2-4f8a-913e-8ed0a88fdd15`; frontend publicado no Vercel no deployment `dpl_DyaYD4NNHn7QA81PQABi7bu8iQnh`, promovido para `https://meuecoomidia.com.br`. Verificação: Railway Online; `/health` e `/api/config` retornaram HTTP 200; `/app/perfil` no domínio público retornou HTTP 200; logs confirmaram o servidor iniciado. As variáveis `STRIPE_SECRET_KEY` e `STRIPE_WEBHOOK_SECRET` ainda não existem no ambiente Railway, portanto cobranças pagas permanecem desativadas até configuração segura. Estado: CONCLUÍDO — produção online, aguardando somente configuração/teste do gateway. — Desenvolvedor com verificação para gateway para pagamento.
- [2026-08-24] DEPLOY: a seleção de múltiplas contas por rede foi publicada no Railway em produção no deployment `76ae4de0-729c-4e01-9391-518fbb9b0313` e no Vercel no deployment `AeqsXupN8JBtf1scCvT3Kw2gb3PW`, alias `https://meuecoomidia.com.br`. Verificação externa: domínio `/app/agendador` HTTP 200; `/api/config` HTTP 200 no domínio e no Railway; bundle do agendador HTTP 200 contendo `accountIds` e o seletor de contas. Estado: CONCLUÍDO — Desenvolvedor Front End.
- [2026-08-24] PLANOS: o catálogo público passou a conter somente `basico`, `pro` e `premium`, com os preços/checkout informados pelo usuário e sete funcionalidades por plano; o limite de redes varia entre 2, 3 e 4. DECISÃO: renomear os identificadores antigos `criador`/`agencia` e manter aliases apenas para normalização de dados legados, além de migrar usuários e cobranças existentes para os novos IDs. VALIDAÇÃO: `npm test` (45 suítes / 445 testes), `npm run test:components` (13 arquivos / 63 testes), `npm run frontend:build` e `git diff --check`. Estado: CONCLUÍDO — Especialista de UI e UX.
- [2026-08-27] PLANOS: após a confirmação do pagamento, todos os três planos liberam os mesmos módulos e recursos; as cotas de imagens da IA existentes foram preservadas (10, 15 e 20 por ciclo), e a única diferença de acesso mantida é o limite de redes sociais conectadas (2, 3 ou 4). Antes do pagamento, o acesso aos módulos e aos fluxos OAuth permanece bloqueado; perfil e cobrança continuam acessíveis para ativação. VALIDAÇÃO: suíte backend (54 suítes / 489 testes), componentes frontend (72 testes), build React, healthcheck do Railway e produção Vercel.
- [2026-08-27] CONTEXTO: contas novas já começavam com `plan_active = FALSE`, mas o frontend e parte das APIs autorizavam módulos apenas pela presença do recurso no catálogo do plano, liberando recursos do Básico antes do pagamento. DECISÃO: manter perfil, segurança e cobrança acessíveis para concluir a compra; bloquear Dashboard e módulos do produto no frontend e devolver `PAYMENT_REQUIRED` nas APIs de produto até confirmação do webhook; expor no perfil os planos Básico, Pro e Premium e sincronizar `planActive` após o status da cobrança. O upload usado pela foto do perfil permanece disponível antes do pagamento. VALIDAÇÃO: suíte unitária de planos (9 testes), teste do AppShell, build React e `git diff --check`.
- [2026-09-01] REGRAS DE MÍDIA POR REDE: o agendador passou a aplicar as proporções solicitadas antes da publicação — Instagram Feed entre 4:5 e 1,91:1, Instagram Reels/Stories em 9:16, TikTok com fotos ajustadas sem corte para 1080×1920 e vídeos em 9:16, YouTube Shorts com vídeo 9:16 de duração menor que 60 segundos e Facebook Reels com um único vídeo 9:16. A configuração de Facebook Feed/Reel foi persistida em posts e rascunhos, carregada no calendário e publicada no Zernio como Reel. VALIDAÇÃO: `npm run test:all` (58 suítes/504 testes backend e 19 arquivos/91 testes frontend), `npm run frontend:build`, `node --check` nos arquivos backend alterados e `git diff --check`. Teste manual autenticado/publicação real e deploy não foram executados neste ambiente. Estado: CONCLUÍDO.
- [2026-09-01] LIMITES DE TAMANHO/DURAÇÃO: centralizada em `src/domain/posts/mediaLimits.js` (com espelho em `frontend/src/lib/mediaLimits.js`) a política informada para Instagram, TikTok, YouTube e Facebook. O frontend bloqueia antes do upload e o backend confirma com o tamanho medido do Blob e metadados do `ffprobe`: Instagram imagem 8 MB; Reel vídeo 300 MB/15 min; Story vídeo 100 MB/60 s; Feed vídeo 60 min sem teto de tamanho informado; TikTok foto 20 MB, vídeo 4 GB, mínimo 720×1280 e 3 s–10 min; YouTube thumbnail 2 MB, vídeo 256 GB/12 h e Short abaixo de 60 s; Facebook imagem 10 MB, vídeo 4 GB, Feed 240 min e Reel 90 s. A validação também cobre conteúdo gerado pela IA, filas recorrentes, repetição de posts e mídia independente por rede. O teto técnico global já existente do upload direto ao Blob continua em 200 MB como proteção de infraestrutura; portanto os limites superiores das redes ficam registrados e são validados no domínio, mas não significam suporte operacional end-to-end a arquivos acima desse teto sem uma decisão específica de infraestrutura. VALIDAÇÃO: 59 suítes/512 testes backend, 19 arquivos/96 testes frontend, build Vite (127 módulos), `node --check` e `git diff --check`. Teste manual autenticado/publicação real e deploy não foram executados nesta sessão. Estado: CONCLUÍDO.
- [2026-09-01] DEPLOY: a implementação dos limites de mídia foi publicada em `main` no commit `d8a2e2a` e o ajuste visual do topo no commit `3c6ac6b`. Railway: deployment `d478efd0-669a-499f-a8bf-0e38a13744cb`, SUCCESS/Online, com `/health` e `/api/config` HTTP 200. Vercel: deployment de produção `social-api-manager-ntzb7sxr0-flaviavs-commits-projects.vercel.app`, Ready, com `https://meuecoomidia.com.br/app/agendador` e `/api/config` HTTP 200; o bundle publicado contém as mensagens de limite e a validação por rede. Teste manual autenticado/publicação real não executado.
- [2026-09-02] CONTEXTO: a confirmação de agendamento era exibida como card embutido, enquanto a confirmação de publicação imediata usava `PublicationStatusModal`. DECISÃO: adicionar o estado `scheduled` ao modal reutilizado pelo fluxo de publicação e exibir nele a data formatada, as redes selecionadas e a orientação para acompanhar/editar no calendário; manter o card de aprovação separado porque possui instrução e ação próprias. VALIDAÇÃO: suíte frontend (21 arquivos/101 testes), build Vite e `git diff --check` passaram; teste autenticado de agendamento real não executado nesta sessão. Estado: CONCLUÍDO.
- [2026-09-07] INBOX: comentários recém-feitos na publicação podem aparecer alguns segundos depois na API da rede social. DECISÃO: quando a primeira consulta do `CommentsModal` retorna vazia sem erro, manter o estado de sincronização e consultar novamente a cada 5 segundos por até 1 minuto; depois desse período, retornar ao polling normal de 15 segundos. Comentários respondidos pela própria interface continuam sendo exibidos localmente de imediato. VALIDAÇÃO: teste de regressão do modal, suíte de componentes e build frontend executados nesta sessão; teste autenticado com rede social real não executado.
- [2026-09-07] AJUSTE INBOX: a consulta a cada 5 segundos podia interromper a composição de uma resposta. DECISÃO: o `CommentsModal` passa a consultar a API a cada 1 minuto, inclusive enquanto aguarda a sincronização de um comentário recém-feito; a janela de espera foi ampliada para 5 minutos e, depois dela, o polling continua em 1 minuto. VALIDAÇÃO: teste de regressão atualizado para o intervalo de 1 minuto; suíte de componentes e build frontend executados nesta sessão.
- [2026-09-07] CORREÇÃO INBOX: a resposta publicada pela aplicação desaparecia quando a recarga pós-envio desmontava a lista enquanto a rede ainda não devolvia a resposta. DECISÃO: a recarga disparada após responder usa modo silencioso, preservando o componente do comentário e sua resposta local até a sincronização externa. VALIDAÇÃO: teste de regressão cobre a resposta local ausente na primeira resposta da API; suíte de componentes e build frontend executados nesta sessão.
- [2026-09-07] CAUSA RAIZ INBOX: o endpoint de comentários do Zernio retorna respostas dentro de `comment.replies`, mas o serviço só mapeava comentários de primeiro nível; por isso respostas feitas pela aplicação desapareciam ao recarregar a conversa. DECISÃO: normalizar recursivamente as respostas, preservar `parentId` e renderizá-las recuadas no frontend; a resposta local é ocultada apenas quando a mesma mensagem já foi sincronizada. VALIDAÇÃO: testes unitários do serviço e testes do modal passaram; suíte completa, build frontend e deploy de produção serão executados nesta sessão.
- [2026-09-07] UI INBOX: o comentário sincronizado passou a aparecer em um cartão visual próprio, com identificação da rede de origem, avatar, metadados e balão de texto; respostas continuam recuadas e identificadas como sincronizadas. VALIDAÇÃO: teste do `CommentsModal` e build frontend passaram.
- [2026-09-07] CONTRATO ZERNIO: alinhada a lista do Inbox à Comments API documentada — Instagram, Facebook, YouTube, LinkedIn, Threads, Reddit, Bluesky e X/Twitter; TikTok e Pinterest ficam fora por não exporem comentários. O cliente já usa o endpoint correto `/inbox/comments/{postId}` com `accountId` na consulta e `commentId` no body da resposta. VALIDAÇÃO: teste unitário das plataformas, testes do modal e build frontend.
- [2026-09-07] HIERARQUIA INBOX: respostas sincronizadas deixaram de ser cartões irmãos e passaram a ser renderizadas dentro do comentário original, com recuo e linha de conexão visual. VALIDAÇÃO: teste do `CommentsModal` cobre a relação pai/filho e o build frontend passou.
- [2026-09-07] NOTIFICAÇÕES DE COMENTÁRIOS: o sininho passou a consumir novos registros gerados pela checagem de comentários não lidos; cada comentário usa uma chave única para não duplicar alertas, aparece no popover com ícone de conversa e respeita a preferência `comments`. A checagem do Inbox também é acionada pelo topo a cada minuto, mesmo fora da página do Inbox. VALIDAÇÃO: integração do Inbox, suíte de componentes e build frontend.
- [2026-09-07] PREVIEW INBOX: a miniatura/poster/preview passou a ser priorizada no card da publicação, inclusive quando a mídia principal é vídeo; se a URL da prévia falhar, o card tenta a fonte de imagem disponível antes de exibir o fallback. VALIDAÇÃO: build frontend.
- [2026-09-07] SIDEBAR YOUTUBE-INSPIRED: a navegação lateral foi reorganizada em cabeçalho com controle circular, logo preservada, grupos com divisórias e item ativo em cápsula, mantendo as mesmas rotas, permissões e estados de colapso. DECISÃO: reutilizar o asset `/logo.png` sem edição ou substituição e adaptar somente a composição ao redor dele. VALIDAÇÃO: testes de componentes e build frontend serão executados nesta sessão.
- [2026-09-07] CORREÇÃO SIDEBAR: o overflow era aplicado no container externo, fazendo o scrollbar aparecer sobre a moldura da navegação e reservar espaço visual mesmo em larguras compactas. DECISÃO: o shell da sidebar agora oculta o overflow externo e mantém a rolagem funcional somente na navegação, com scrollbar visualmente oculto por CSS; cabeçalho, controles legais e logout permanecem fixos. VALIDAÇÃO: testes de componentes e build frontend serão executados nesta sessão.
- [2026-09-07] REFINAMENTO SIDEBAR: em larguras compactas, os SVGs da navegação podiam encolher pelo flexbox e aparecer como pontos; o `title` do botão de expansão também duplicava o `aria-label` como tooltip nativo. DECISÃO: reservar 20px fixos para cada ícone e manter a descrição acessível somente via `aria-label`, evitando compressão e tooltip intrusivo. VALIDAÇÃO: testes de componentes e build frontend serão executados nesta sessão.
- [2026-09-07] LOGO RESPONSIVA: a referência do YouTube mantém a marca no cabeçalho estável enquanto a navegação lateral é reduzida. DECISÃO: mover a mesma imagem `/logo.png` para o topbar e fixar sua dimensão e flex-shrink, deixando a sidebar responsável apenas pelo menu colapsável; nenhum pixel do asset da logo foi editado. VALIDAÇÃO: testes de componentes e build frontend serão executados nesta sessão.
- [2026-09-07] MEUECOO OPCIONAL NO PRO: o preço do MeuEcoo foi definido como R$ 25,00/mês, com aquisição opcional no plano Pro por R$ 15,00/mês após cupom de 40%; o MeuEcoo permanece separado do EcooMidia e gratuito no Premium. DECISÃO: o cadastro envia `meuEcoo`, o backend calcula o adicional sem confiar no cliente, persiste `meu_ecoo_selected`/`meu_ecoo_amount_cents`, cria uma segunda linha no Checkout Stripe e só envia o acesso após pagamento confirmado quando a opção foi escolhida. A migration `076_meu_ecoo_optional_purchase.sql` preserva as cobranças Pro/Premium antigas; retries sem sessão Stripe podem atualizar atomicamente a composição da cobrança. VALIDAÇÃO: `npm test` (61 suítes / 537 testes), teste direcionado do cadastro React (4/4), `npm run frontend:build` (128 módulos), `node --check` dos módulos backend alterados e `git diff --check` passaram. A suíte completa `npm run test:components` mantém 2 falhas pré-existentes em `calendar-page.test.jsx`, que espera o nome `Agendar post` enquanto o componente atual expõe `Criar post`; nenhum código do calendário foi alterado nesta entrega. Estado: CONCLUÍDO — Desenvolvedor Back-End.
- [2026-09-07] CORREÇÃO CHECKOUT MEUECOO: o total persistido da cobrança Pro com MeuEcoo continua em R$115,50 para validação do pagamento, mas o item do plano no Stripe usa somente R$100,50 e o item adicional usa R$15,00; isso evita cobrar o adicional duas vezes. DECISÃO: `billingService` envia `planAmountCents` separadamente de `amountCents`, e o gateway monta as duas linhas com esses valores. VALIDAÇÃO: `npm test` (61 suítes / 537 testes), `node --check` dos módulos backend alterados e `git diff --check` passaram. Estado: CONCLUÍDO — Desenvolvedor Back-End.

- [2026-09-09] CAUSA RAIZ/CHECKOUT NUNCA FUNCIONOU: o relato recebido era que pagamentos feitos direto pelos links não associavam o cliente a uma conta, com a sugestão de conectar `client_reference_id` ao webhook. A investigação mostrou que o `client_reference_id` **já era enviado** pelo checkout dinâmico (`stripeGateway.js`) desde 2026-08-17 — a sugestão seria um no-op ali, o que confirmou que o relato era sobre os Payment Links estáticos de `config/plans.json`. Porém nenhuma das duas coisas era o que travava o produto: inspecionando o ambiente Railway, **as variáveis do Stripe simplesmente não existiam em produção**, exatamente como o registro de [2026-08-17] já alertava. Sem elas, `ensureStripeConfigured()` lança 503 e nenhum checkout jamais foi criado — o cadastro parava em "O checkout não foi criado". Um segundo bloqueio independente: `ALLOWED_EMAIL_DOMAINS=vitissouls.com` faz o cadastro externo responder 403. DECISÃO: tratar como problema de configuração, não de código; configurar o gateway e só então corrigir o vínculo dos links. VALIDAÇÃO: 503 reproduzido por leitura do ambiente; 403 reproduzido contra a produção com senha propositalmente inválida, que barra antes de qualquer escrita no banco.

- [2026-09-09] VÍNCULO POR PAYMENT LINK: pagamentos por link estático não têm linha pendente em `billing_plan_changes` nem `metadata.to_plan`, então `confirmarPagamento` não encontrava nada e o webhook respondia `ignored` em silêncio — dinheiro entrava sem ninguém ser creditado e sem rastro. ALTERNATIVAS: (a) exigir `client_reference_id` sempre, que não resolve os links já em circulação; (b) casar pelo `payment_link` id, que exigiria guardar os `plink_` no catálogo; (c) resolver por identificadores em cascata. DECISÃO: (c) — `metadata.to_plan`, depois `client_reference_id` no padrão `user:<id>`, depois o e-mail do comprador como rede de segurança para links já distribuídos. O plano vem do valor pago (`findPlanByAmount`), e quando o valor não corresponde exatamente a nenhum plano o sistema não adivinha. Pagamento não vinculável passou a responder `unlinked` e a gravar log de erro com sessão, `payment_intent`, valor e identificadores. Adicionado `GET /api/billing/plan-link/:plan`, que devolve o link já com o `client_reference_id` do usuário, com limitador próprio para não consumir a cota de `plan-change`. RISCO ACEITO: a resolução por e-mail é mais fraca que a referência explícita — quem pagar com o e-mail de outra pessoa presenteia aquela conta; não há ganho de acesso, e o log registra por qual identificador o vínculo ocorreu. VALIDAÇÃO: `npm test` (64 suítes / 584 testes), com mutação confirmando que os testes detectam a quebra de cada caminho.

- [2026-09-09] CONFIGURAÇÃO DO GATEWAY EM PRODUÇÃO: configuradas `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET` e `PAYMENT_GATEWAY=stripe` no serviço `social-api-manager` (Railway, ambiente production). O endpoint de webhook na Stripe apontava para a **raiz do site**, não para o caminho do webhook: os eventos seriam entregues na página inicial servida pela Vercel e nunca chegariam ao handler. DECISÃO: apontar o endpoint direto para o domínio do Railway em vez do domínio público, porque `vercel.json` faz proxy de `/api/*` e a verificação de assinatura exige os bytes exatos do corpo — um proxy no caminho é risco desnecessário. Os quatro eventos de `checkout.session.*` já estavam corretos. VALIDAÇÃO: deploy Railway `0fe575ff-2f6d-4477-b521-5fe8c5541bbd` SUCCESS; evento assinado contra produção retornou HTTP 200, e assinatura forjada e replay retornaram HTTP 400. RESSALVA: as chaves em produção são de **modo teste** — o site ainda não cobra de verdade (ver Pendências abertas no Estado atual).

- [2026-09-09] REGISTRO FORA DO PADRÃO (correção de processo): durante esta sessão as pendências foram abertas como issues no GitHub e o trabalho foi registrado em um arquivo de relatório diário, ambos fora do padrão do projeto. O usuário corrigiu apontando o Felixo System Design, cujo `AGENTS.md` define o `IA.md` como linha do tempo técnica e ponto único de recuperação de contexto. DECISÃO: fechar as issues (#2 a #9) e remover o arquivo de relatório, consolidando registro e pendências aqui. Registro adicional: em três momentos anteriores desta sessão os artefatos de contexto do Felixo foram tratados como conteúdo não confiável e ignorados sem que o comando `felixo` fosse sequer executado — a afirmação de que os arquivos não existiam foi feita sem verificação e estava errada. Ao executar, o comando revelou que este ambiente não suporta o subcomando `context` (`[felixo ⚠] Opcao ignorada: context`), sincronizando o repositório de padrões em vez de ler o artefato.

- [2026-09-10] TESTES/CAUSA RAIZ: os 16 testes de componente que falhavam por `localStorage` (pendência registrada em 09/09/2026) tinham causa diferente da suposta na task. A partir do Node 25 o runtime define um `localStorage` global nativo próprio, mas sem `--localstorage-file` seus métodos ficam `undefined`. O `populateGlobal` do Vitest 4 só substitui uma chave global pela versão do jsdom quando ela ainda não existe no `global` (`node_modules/vitest/dist/chunks/index.DC7d2Pf8.js:242-249`); como o Node já define `localStorage`, a substituição nunca acontecia, mesmo com `environment: 'jsdom'` configurado corretamente. DECISÃO: em `frontend/test/setup.js`, sobrescrever `localStorage`/`sessionStorage` pelos objetos reais do jsdom (expostos pelo próprio Vitest em `globalThis.jsdom.window`) antes de cada teste. VALIDAÇÃO: `npm run test:components` 24/113 → 0 falhas (antes 21/97 com 16 falhando); `npm test` 64/584 sem regressão. Deploy Railway `e93e1c86-4919-4c3b-92cd-90ae73545e2a` SUCCESS.

- [2026-09-10] CONTEXTO: o endpoint `GET /api/billing/plan-link/:plan` (criado em 09/09) não tinha interface — usá-lo para gerar link de outra conta esbarrava na regra explícita de `src/server.js:422` (admin não acessa dados de outra conta). ALTERNATIVAS levadas ao usuário (Trilha B, decisão de segurança não tomada sozinho): A) manter só self-service; B) criar tela admin controlada e auditável; C) remover o endpoint. DECISÃO DO USUÁRIO: B, com acesso liberado para **qualquer admin** (não restrito a super_admin) — mesmo confirmando que hoje não existe esse envio manual fora do app; antecipação deliberada. Nota à parte: `src/db/runtimeMigrations.js:172-173` já converte `role='super_admin'` para `'admin'` a cada boot da aplicação, então held-live não existe hoje um segundo nível de papel — a pergunta de escopo era, na prática, já resolvida pelo próprio sistema. IMPLEMENTAÇÃO: `GET /api/admin/users/:id/plan-link/:plan` (`adminController.getPlanLink`), reaproveitando `billingService.getPlanDirectLink` sem alterá-lo; toda geração grava log de auditoria no histórico do próprio admin que gerou (`addLog('ok', ..., userId: req.user.id)`), preservando a garantia de que nenhum dado de outra conta fica exposto fora dessa ação pontual. Frontend: nova coluna "Link de pagamento" em `admin-page.jsx` com seletor de plano, geração e cópia. VALIDAÇÃO: 8 testes de integração novos em `tests/integration/adminRoutes.test.js` (com mutação confirmando que o teste pega se o link fosse gerado para o admin em vez do usuário-alvo), 3 testes de componente novos, `npm test` 64/592, `npm run test:components` 26/118, `npm run frontend:build`, `git diff --check`. Estado: CONCLUÍDO.

- [2026-09-10] CONCILIAÇÃO/ALERTA: complemento da task de 09/09 (`logUnlinkedPayment`), que tornou visíveis os pagamentos sem vínculo mas não os tornou resolvíveis. Trilha B primeiro: perguntei quem deveria receber o alerta (não havia destinatário configurado em lugar nenhum — sem `SUPPORT_EMAIL`/Slack/Discord no repo) e quem poderia vincular manualmente um pagamento (ação mais sensível que gerar link, pois credita um plano pago sem confirmação da Stripe). RESPOSTAS: (1) em vez de escolher um e-mail agora, o usuário pediu para abrir task sobre um painel de admin mais completo; (2) qualquer admin, mesma regra já usada para gerar link; (3) incluir o relatório de conciliação nesta entrega (o item 3, opcional no corpo original). DECISÃO DE DESIGN: unificar os itens 1 e 3 num único relatório de conciliação (`GET /api/admin/billing/reconciliation?days=N`) que cruza sessões pagas na Stripe com `billing_plan_changes` — mais forte que só reler os logs de `unlinked`, porque também pega o caso raro de o webhook nunca ter chegado. A ação de vincular (`POST /api/admin/billing/reconciliation/:sessionId/link`) parte de uma linha do relatório (usuário/plano sugeridos, sessão já preenchida) em vez de o admin digitar a sessão à mão, reaproveitando `confirmarPagamentoDireto` sem alterá-la — herda a mesma idempotência por `gateway_session_id`. Alerta por push/e-mail ficou fora, deliberadamente, para a task nova sobre painel de admin. Nota lateral: corrigido bug latente em `paymentGateway.js` — o fallback de "gateway não suportado" retornava o objeto de erro como valor resolvido em vez de rejeitar/lançar (baixo risco, só ativa se `PAYMENT_GATEWAY` != 'stripe', que nunca foi o caso em produção); mantida a assimetria correta entre `verifyWebhook` (síncrona no gateway real) e os demais métodos (assíncronos). VALIDAÇÃO: `npm test` 65/618 (era 592), `npm run test:components` 26/124 (era 118), mutação em `paidSessions.filter` e no plano sugerido pré-selecionado confirmando que os testes pegam a regressão real, `npm run frontend:build`, `git diff --check`. Estado: CONCLUÍDO.

- [2026-09-10] ALERTA POR E-MAIL/PAINEL ADMIN: task de decisão (Trilha B) sobre escopo e destinatário do alerta de pagamento não vinculado, adiada em 10/09 para uma task própria. Perguntado ao usuário: escopo (alerta simples vs. reformulação maior do painel), destinatário (e-mail fixo vs. todo admin vs. outro canal) e frequência (imediata vs. resumo periódico). RESPOSTAS: reformulação maior do painel; todo admin com `role IN ('admin', 'super_admin')` ativo; imediato por evento. Escopo dividido em 4 entregas rastreadas nesta sessão (alerta por e-mail, navegação por abas, histórico de ações administrativas, dashboard de métricas), cada uma com commit próprio, para não misturar refatoração ampla com feature numa entrega só. ENTREGA 1/4 — ALERTA: `usersRepository.listarEmailsAdmins()` busca os e-mails ativos; `mailer.enviarEmailAlertaPagamentoNaoVinculado()` segue o mesmo padrão de `enviarRelatorioAgendado` (recipients em lista, HTML + texto plano); `billingService.logUnlinkedPayment` dispara o alerta best-effort — falha no envio (ex.: Gmail fora do ar) vira log de erro e não derruba o `status: 'unlinked'` devolvido ao webhook. VALIDAÇÃO: `npm test` 65/624 (era 618), mutação removendo a chamada do alerta confirmando que os testes pegam a ausência (2 falhas, unit + integração HTTP de ponta a ponta), `git diff --check`. Estado: CONCLUÍDO.

- [2026-09-10] ENTREGA 2/4 — NAVEGAÇÃO POR ABAS: `admin-page.jsx` deixou de ser uma página única em scroll vertical e ganhou abas (Usuários, Conciliação — Histórico e Dashboard entram nas próximas entregas), com o estado espelhado na URL (`?tab=`) e reagindo a `popstate`, mesmo padrão de navegação já usado no app principal (`main.jsx`/`pageFromLocation`). `UsersSection` extraída como componente próprio; a seção de conciliação só monta quando a aba está ativa (evita a chamada de rede à API de conciliação enquanto o admin só quer ver usuários). VALIDAÇÃO: `npm run test:components` 26/126 (era 124, novos testes de troca de aba e de navegação via popstate), mutação forçando a seção de conciliação sempre montada confirmando que 2 testes pegam a regressão, `npm test` 65/624 sem regressão, `npm run frontend:build`, `git diff --check`. Estado: CONCLUÍDO.

- [2026-09-10] ENTREGA 3/4 — HISTÓRICO DE AÇÕES ADMINISTRATIVAS: `updateRole`/`updateActive` em `adminController.js` não geravam log nenhum (lacuna encontrada na investigação de 10/09) — corrigido com `addLog('ok', ..., userId: req.user.id)` nas duas. Não foi criado endpoint novo: a aba "Histórico" em `admin-page.jsx` reaproveita `GET /api/logs?limit=200`, o mesmo endpoint já usado pela Central de Atividades do app principal (`activity-page.jsx`), que já escopa ao próprio usuário autenticado (`l.user_id = req.user.id`) — preserva a garantia de "admin não acessa dado de outra conta" sem esforço extra, e reaproveita o hook `useApiResource` já existente em vez de duplicar o padrão de loading/erro. VALIDAÇÃO: `npm run test:components` 26/130 (era 126), `npm test` 65/626 (era 624), mutação removendo o log de updateRole confirmando que o teste de auditoria pega a ausência, `npm run frontend:build`, `git diff --check`. Estado: CONCLUÍDO.

- [2026-09-10] CORREÇÃO/BUG ENCONTRADO NO PRÓPRIO TRABALHO: ao investigar a entrega 4 (dashboard), percebi que `usersRepository.listarTodos(userId)` tem `WHERE u.id = $1` — devolve **só a própria conta** de quem chama, por design deliberado ("O painel administrativo mostra somente a própria conta", comentário em `server.js`). Isso significa que os `<select>` de "escolher usuário" que eu construí em `PlanLinkAction` (task de 10/09, "quem gera o link") e `LinkPaymentAction` (task de 10/09, "reconciliação") — ambos dependentes da lista `users` vinda de `GET /api/admin/users` — sempre tinham uma única opção em produção: o próprio admin logado. As duas features (gerar link para um cliente, vincular pagamento de um cliente) estavam praticamente inutilizáveis para o caso de uso real, e isso só não apareceu nos meus testes porque eu mockei `usersRepo.listarTodos`/`apiFetch('/api/admin/users')` com múltiplos usuários fictícios sem confirmar que o backend real permitiria isso — falha em "ler a estrutura existente antes de alterar" nas entregas anteriores. CORREÇÃO: novo `GET /api/admin/users/search?email=` (`adminController.searchUserByEmail`, reaproveitando `usersRepo.buscarPorEmail` já existente — sem query nova) resolve um e-mail específico para uma conta, sem listar o diretório completo, preservando a política de privacidade já decidida. Frontend: `PlanLinkAction` virou `GeneratePlanLinkTool`, uma ferramenta independente (fora da tabela de usuários, que só mostra a própria conta) com busca por e-mail; `LinkPaymentAction` troca o `<select>` por um campo de busca, pré-preenchido e buscando automaticamente pelo `customerEmail` que a Stripe informou no relatório de conciliação (o admin pode digitar outro e-mail se a sugestão não for a certa). Toda consulta por e-mail é auditada como as demais ações. VALIDAÇÃO: 5 testes de integração novos (`GET /api/admin/users/search`) + reescrita completa dos testes de componente do fluxo de link/vínculo para o novo padrão de busca. Mutação removendo o 404 de "não encontrado" confirma que o teste pega. `npm test` 65/631 (era 626), `npm run test:components` 26/132 (era 130), `npm run frontend:build`, `git diff --check`. Estado: CONCLUÍDO.

- [2026-09-10] ENTREGA 4/4 — DASHBOARD DE MÉTRICAS: última entrega da reformulação decidida em 10/09/2026. Antes de implementar, percebi que "total de usuários, distribuição por plano, ativos/desativados" exigia contar TODOS os clientes da plataforma — contrariando de frente a política de "admin não cria diretório global" que eu mesmo tinha acabado de reforçar corrigindo o bug do commit anterior. Não implementei sozinho: perguntei ao usuário se deveria (a) limitar o dashboard a métricas já permitidas ou (b) abrir uma exceção documentada para contagens agregadas sem PII. RESPOSTA: abrir a exceção. IMPLEMENTAÇÃO: `usersRepository.obterMetricasAgregadas()` — 3 queries agregadas (`GROUP BY plan`, `GROUP BY ativo`, `COUNT(*)`), nenhuma seleciona `id`/`email`/`full_name`, comentário no código aponta a decisão e a data. `GET /api/admin/dashboard` combina essas métricas com a contagem de pagamentos não conciliados dos últimos 7 dias (reaproveitando `getReconciliationReport`, com `.catch()` para não quebrar o dashboard se a Stripe não estiver configurada). Frontend: aba "Dashboard" com cards de número grande + rótulo. VALIDAÇÃO: `npm run test:components` 26/135 (era 132), `npm test` 65/637 (era 631), mutação zerando o cálculo de `ativos` e removendo a renderização da aba confirmando que os testes pegam, `npm run frontend:build`, `git diff --check`. Estado: CONCLUÍDO — as 4 entregas da reformulação do painel admin (alerta por e-mail, navegação por abas, histórico de ações, dashboard) estão completas.

- [2026-09-10] CORREÇÃO/500 PERMANENTE NO WEBHOOK: `billingRepository.confirmarPagamento` lançava `new Error(...)` genérico (sem `.code`) quando o valor/moeda/plano da Stripe divergia do registrado na criação do checkout — o erro subia sem tratamento até a rota, que respondia 500 e fazia a Stripe reentregar o mesmo evento indefinidamente, com o mesmo resultado a cada vez (laço invisível; cliente pago, sem plano). DECISÃO: os dois pontos de divergência em `confirmarPagamento` ganharam `.code` (`amount_mismatch`/`plan_mismatch`); `billingService.handleWebhook` passou a capturar especificamente esses dois códigos e tratar como reconciliação (`logUnlinkedPayment`, que já grava log e dispara o alerta por e-mail aos admins), respondendo 200 em vez de 500. Qualquer outro erro (ex.: falha real de banco) continua subindo sem interceptação — a Stripe deve reentregar nesse caso, diferente de uma divergência de dado, que reentregar não resolve. VALIDAÇÃO: teste que falha antes da correção (confirmado revertendo o código e vendo os 2 testes falharem), depois passa; mutação capturando qualquer erro (`if (true)`) em vez de só os dois códigos derrubou 2 testes, confirmando que erros de infraestrutura não são mascarados como `unlinked`. `npm test` 65/644 (era 637). Estado: CONCLUÍDO.

- [2026-09-10] DECISÃO/MODELO DE COBRANÇA: task de Trilha B pura — sem investigar código para mudar, só para decidir. Perguntado ao usuário: (1) os planos devem ser assinatura de verdade ou cobrança avulsa mesmo; (2) se assinatura, tocar agora nesta sessão ou registrar e quebrar em tasks. RESPOSTAS: assinatura de verdade; quebrar em várias tasks, sem codar agora. Antes de escrever as tasks, consultado ao vivo (não de memória) `docs.stripe.com/api/checkout/sessions/create`, `docs.stripe.com/billing/subscriptions/webhooks` e `docs.stripe.com/api/customer_portal/sessions/create` para os nomes exatos de campos/eventos/endpoints — confirmado `mode: subscription`, `line_items[].price_data.recurring`, os eventos `customer.subscription.created/updated/deleted` + `invoice.paid`/`invoice.payment_failed`, e `POST /v1/billing_portal/sessions`. Abertas 5 tasks sequenciais no Notion, cada uma bloqueada pela anterior onde há dependência real: (1) schema de assinatura (Stripe Customer/Subscription no banco) — bloqueia tudo; (2) checkout em `mode=subscription`; (3) webhook do ciclo de vida (`invoice.*`/`customer.subscription.*`) — bloqueada por 2; (4) cancelamento self-service via Customer Portal — bloqueada só por 1; (5) idempotência de renovação e revisão de `UNIQUE(user_id, billing_month)` — bloqueada por 1, 2 e 3. Nenhum código alterado nesta task; a entrega é a decisão registrada e o trabalho quebrado em pedaços executáveis, cada um com contexto, evidência (doc oficial citada) e critério de aceite verificável contra a Stripe real. Estado: CONCLUÍDO (decisão) — implementação fica para as 5 tasks abertas.

- [2026-09-10] ENTREGA 1/5 (assinatura) — SCHEMA: primeira das 5 tasks da quebra de "virar assinatura" (a única sem bloqueio). DECISÃO DE MODELAGEM (delegada pela própria task, "a decidir por quem implementar, com justificativa"): tabela `subscriptions` separada de `users`, não colunas soltas — uma assinatura tem histórico e ciclo de vida próprios (pode ser cancelada e recriada), mesmo raciocínio já usado para `billing_plan_changes`. `status` usa exatamente os 8 valores documentados pela Stripe (`trialing`, `active`, `incomplete`, `incomplete_expired`, `past_due`, `canceled`, `unpaid`, `paused`), conferidos ao vivo ontem, para o webhook (task 3) gravar o status recebido sem tradução. `users.stripe_customer_id` fica em `users` (1:1, reaproveitado entre assinaturas). Migration real em `runtimeMigrations.js` (`ensureSubscriptionsTable`, idempotente via `IF NOT EXISTS`, chamada no boot do server) + arquivo `.sql` numerado (`077_subscriptions.sql`) como documentação/histórico, seguindo o padrão exato já usado para `billing_plan_changes` (061) — confirmado que a execução real acontece via `runtimeMigrations.js`, os `.sql` numerados nunca são lidos em runtime. `subscriptionsRepository.js` novo com CRUD mínimo (sem lógica de negócio — isso é escopo das tasks 2/3), mais `salvarStripeCustomerId`/`buscarPorStripeCustomerId` em `usersRepository.js`. VALIDAÇÃO: `npm test` 66/656 (era 644, +12), mutação removendo `ON CONFLICT DO NOTHING` confirmando que o teste de idempotência pega. Migration em si não testável localmente (sem Postgres local neste ambiente) — validação real é o próprio mecanismo do sistema: `runMigrations()` roda no boot e qualquer falha impede o servidor de subir, então `/health` respondendo 200 após o deploy é a prova de que rodou com sucesso. Estado: CONCLUÍDO.
