import { useCallback, useEffect, useState } from 'react'
import { apiFetch } from '../lib/api.js'
import { useToast } from '../components/ui/toast.jsx'

export function SmartlinksPage() {
  const [smartlinks, setSmartlinks] = useState([])
  const [form, setForm] = useState({ name: '', title: '', description: '', links: '' })
  const notify = useToast()
  const load = useCallback(() => apiFetch('/api/smartlinks').then(data => setSmartlinks(data.smartlinks || [])), [])
  useEffect(() => { load().catch(error => notify(error.message, 'error')) }, [load, notify])
  async function save(event) { event.preventDefault(); try { const items = form.links.split('\n').map(line => { const [label, url] = line.split('|'); return { label: label?.trim(), url: url?.trim() } }).filter(item => item.label && item.url); await apiFetch('/api/smartlinks', { method: 'POST', body: JSON.stringify({ name: form.name, title: form.title, description: form.description, items }) }); setForm({ name: '', title: '', description: '', links: '' }); await load(); notify('Smartlink criado.') } catch (error) { notify(error.message, 'error') } }
  async function remove(id) { if (!window.confirm('Excluir este Smartlink?')) return; try { await apiFetch(`/api/smartlinks/${id}`, { method: 'DELETE' }); setSmartlinks(current => current.filter(item => item.id !== id)); notify('Smartlink removido.') } catch (error) { notify(error.message, 'error') } }
  return <section className="page-view"><header className="panel-heading"><div><p className="eyebrow">CONVERSÃO</p><h2>Smartlinks</h2><p className="panel-subtitle">Transforme o link da bio em uma página rastreável para seus principais destinos.</p></div></header><div className="grid gap-5 lg:grid-cols-[minmax(0,1fr)_minmax(0,1.2fr)]"><form className="panel space-y-4" onSubmit={save}><div><p className="eyebrow">NOVO SMARTLINK</p><h3 className="text-lg font-semibold text-zinc-100">Criar página de links</h3></div><label className="block text-sm text-zinc-300">Nome interno<input className="mt-1 w-full rounded-lg border border-subtle bg-app px-3 py-2 text-sm text-zinc-100" value={form.name} onChange={event => setForm(current => ({ ...current, name: event.target.value }))} placeholder="Perfil principal" required/></label><label className="block text-sm text-zinc-300">Título público<input className="mt-1 w-full rounded-lg border border-subtle bg-app px-3 py-2 text-sm text-zinc-100" value={form.title} onChange={event => setForm(current => ({ ...current, title: event.target.value }))} placeholder="Meu Ecoo Mídia"/></label><label className="block text-sm text-zinc-300">Descrição<textarea className="mt-1 w-full rounded-lg border border-subtle bg-app px-3 py-2 text-sm text-zinc-100" value={form.description} onChange={event => setForm(current => ({ ...current, description: event.target.value }))} placeholder="Uma frase sobre você ou sua marca"/></label><label className="block text-sm text-zinc-300">Links <small className="text-zinc-500">um por linha: texto | https://endereco.com</small><textarea className="mt-1 min-h-36 w-full rounded-lg border border-subtle bg-app px-3 py-2 text-sm text-zinc-100" value={form.links} onChange={event => setForm(current => ({ ...current, links: event.target.value }))} placeholder={'Meu site | https://exemplo.com\nInstagram | https://instagram.com/'} required/></label><button className="action-button">Criar Smartlink</button></form><section className="panel"><div className="panel-heading"><div><p className="eyebrow">PÁGINAS PUBLICADAS</p><h3>Seus Smartlinks</h3></div><span className="status-badge">{smartlinks.length}</span></div>{smartlinks.length ? <div className="space-y-3">{smartlinks.map(link => <article className="rounded-xl border border-subtle bg-app p-4" key={link.id}><div className="flex items-start justify-between gap-3"><div><strong className="text-zinc-100">{link.title || link.name}</strong><p className="mt-1 text-sm text-zinc-400">/go/{link.slug} · {link.items?.length || 0} links</p><div className="mt-2 flex flex-wrap gap-2">{(link.items || []).map(item => <span className="rounded-full bg-gold/10 px-2 py-1 text-xs text-gold" key={item.id}>{item.label} · {item.clicks || 0} cliques</span>)}</div></div><button className="link-button danger-link" type="button" onClick={() => remove(link.id)}>Excluir</button></div><a className="mt-3 inline-block text-sm text-gold hover:underline" href={`/go/${link.slug}`} target="_blank" rel="noreferrer">Abrir página pública →</a></article>)}</div> : <p className="empty-state">Crie sua primeira página de links.</p>}</section></div></section>
}
