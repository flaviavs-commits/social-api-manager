import { useEffect, useState } from 'react'
import { apiFetch } from '../lib/api.js'
import { SchedulerPage } from './scheduler-page.jsx'
import { CalendarPage } from './calendar-page.jsx'
const descriptions={agendador:['Novo post','Crie uma publicação para suas redes conectadas.'],calendario:['Calendário','Visualize suas publicações agendadas.'],rascunhos:['Rascunhos','Continue trabalhando nas publicações salvas.'],analytics:['Analytics','Acompanhe o desempenho das suas publicações.'],inbox:['Inbox','Gerencie comentários e interações em um só lugar.'],integracoes:['Contas conectadas','Conecte e gerencie suas redes sociais.'],tokens:['Tokens','Gerencie tokens de acesso e integrações.'],ai:['Assistente IA','Use o assistente para planejar e revisar conteúdos.']}
function DraftsPage() { const [drafts,setDrafts]=useState([]); const [text,setText]=useState(''); const load=()=>apiFetch('/api/drafts').then(data=>setDrafts(data.drafts||[])); useEffect(()=>{load()},[]); async function save(event){event.preventDefault();if(!text.trim())return;await apiFetch('/api/drafts',{method:'POST',body:JSON.stringify({title:'Rascunho',text,platforms:[]})});setText('');load()} async function remove(id){await apiFetch(`/api/drafts/${id}`,{method:'DELETE'});load()} return <section className="page-view"><section className="panel"><p className="eyebrow">EDITOR</p><h2>Novo rascunho</h2><form className="draft-form" onSubmit={save}><textarea value={text} onChange={event=>setText(event.target.value)} placeholder="Escreva o conteúdo da publicação..." aria-label="Texto do rascunho"/><button className="action-button">Salvar rascunho</button></form></section><section className="panel"><h2>Rascunhos salvos</h2>{drafts.length?drafts.map(draft=><div className="data-row" key={draft.id}><span>{draft.text||draft.title}</span><button className="link-button" onClick={()=>remove(draft.id)}>Excluir</button></div>):<p className="empty-state">Nenhum rascunho salvo.</p>}</section></section> }
function AccountsPage() { const [accounts,setAccounts]=useState([]); const [error,setError]=useState(''); const load=()=>apiFetch('/api/accounts').then(data=>setAccounts(data.data||[])).catch(e=>setError(e.message)); useEffect(()=>{load()},[]); async function remove(id){await apiFetch(`/api/accounts/${id}`,{method:'DELETE'});load()} return <section className="page-view"><section className="panel"><p className="eyebrow">INTEGRAÇÕES</p><h2>Contas conectadas</h2>{error&&<p className="error-message">{error}</p>}{accounts.length?accounts.map(account=><div className="data-row" key={account.id}><span>{account.name||account.handle||account.platform}</span><button className="link-button" onClick={()=>remove(account.id)}>Desconectar</button></div>):<p className="empty-state">Nenhuma conta conectada.</p>}</section></section> }
function AnalyticsPage() { const [data,setData]=useState(null); const [error,setError]=useState(''); useEffect(()=>{apiFetch('/api/posts/analytics').then(setData).catch(e=>setError(e.message))},[]); const values=Object.entries(data||{}).filter(([,value])=>typeof value==='number'); return <section className="page-view"><section className="panel"><p className="eyebrow">DESEMPENHO</p><h2>Analytics</h2>{error?<p className="error-message">{error}</p>:data?<div className="metric-grid">{values.length?values.map(([key,value])=><article key={key}><span>{key.replaceAll('_',' ')}</span><strong>{value}</strong></article>):<p className="empty-state">Ainda não há métricas disponíveis.</p>}</div>:<p className="empty-state">Carregando métricas...</p>}</section></section> }
function InboxPage() { const [posts,setPosts]=useState([]); useEffect(()=>{apiFetch('/api/posts/inbox').then(data=>setPosts(data.posts||[])).catch(()=>{})},[]); return <section className="page-view"><section className="panel"><p className="eyebrow">INTERAÇÕES</p><h2>Inbox</h2>{posts.length?posts.map(post=><div className="data-row" key={post.id}><span>{post.text||post.title||'Publicação'}</span><small>{post.comments_count||post.commentCount||0} comentários</small></div>):<p className="empty-state">Nenhuma interação encontrada.</p>}</section></section> }
function TokensPage() { const [tokens,setTokens]=useState([]); const load=()=>apiFetch('/api/tokens').then(data=>setTokens(data.tokens||[])).catch(()=>{}); useEffect(()=>{load()},[]); async function remove(id){await apiFetch(`/api/tokens/${id}`,{method:'DELETE'});load()} return <section className="page-view"><section className="panel"><div className="panel-heading"><div><p className="eyebrow">CREDENCIAIS</p><h2>Tokens de acesso</h2></div><button className="action-button" onClick={()=>apiFetch('/api/tokens/renew-all',{method:'POST'}).then(load)}>Renovar todos</button></div>{tokens.length?tokens.map(token=><div className="data-row" key={token.id}><span>{token.account_name||token.platform}</span><small>{token.status||'valid'}</small><button className="link-button" onClick={()=>remove(token.id)}>Remover</button></div>):<p className="empty-state">Nenhum token cadastrado.</p>}</section></section> }
function AiPage() { const [instruction,setInstruction]=useState(''); const [posts,setPosts]=useState([]); const [error,setError]=useState(''); const [loading,setLoading]=useState(false); async function generate(event){event.preventDefault();setLoading(true);setError('');try{const data=await apiFetch('/api/ai/generate',{method:'POST',body:JSON.stringify({instrucao:instruction,plataformas:['instagram'],quantidade:3,tom:'profissional'})});setPosts(data.posts||[])}catch(e){setError(e.message)}finally{setLoading(false)}} return <section className="page-view"><section className="panel"><p className="eyebrow">ASSISTENTE</p><h2>Gerar ideias de posts</h2><form className="draft-form" onSubmit={generate}><textarea value={instruction} onChange={event=>setInstruction(event.target.value)} placeholder="Ex.: crie ideias sobre educação financeira" aria-label="Instrução para a IA"/><button className="action-button" disabled={loading}>{loading?'Gerando...':'Gerar posts'}</button></form>{error&&<p className="error-message">{error}</p>}</section>{posts.length>0&&<section className="panel"><h2>Sugestões</h2>{posts.map((post,index)=><div className="data-row" key={post.id||index}><span>{post.text||post.caption||JSON.stringify(post)}</span></div>)}</section>}</section> }
export function ModulePage({type}) { if(type==='rascunhos')return <DraftsPage/>; if(type==='integracoes')return <AccountsPage/>; if(type==='calendario')return <CalendarPage/>; if(type==='analytics')return <AnalyticsPage/>; if(type==='inbox')return <InboxPage/>; if(type==='tokens')return <TokensPage/>; if(type==='ai')return <AiPage/>; if(type==='agendador')return <SchedulerPage/>; const [title,description]=descriptions[type]||['Módulo','Área da aplicação']; return <section className="page-view"><section className="panel module-placeholder"><p className="eyebrow">MÓDULO REACT</p><h2>{title}</h2><p>{description}</p><span className="status-badge">Migração em andamento</span></section></section> }
