import { useState } from 'react'
const navigation = [['dashboard','Visão geral'],['agendador','Novo post'],['calendario','Calendário'],['rascunhos','Rascunhos'],['analytics','Analytics'],['inbox','Inbox'],['integracoes','Contas conectadas'],['tokens','Tokens'],['ai','Assistente IA']]
export function AppShell({ page, onPageChange, children, user }) { const [open,setOpen]=useState(false); return <div className="app-shell"><aside className={`app-sidebar ${open?'is-open':''}`}><div className="app-brand"><img src="/favicon.svg" alt=""/><span>Meu Ecoo Mídia</span></div><nav aria-label="Navegação principal">{navigation.map(([key,label])=><button key={key} className={page===key?'is-active':''} onClick={()=>{onPageChange(key);setOpen(false)}}>{label}</button>)}</nav><button className="logout-button" onClick={()=>{localStorage.removeItem('authToken');window.location.href='/login.html'}}>Sair</button></aside><div className="app-content"><header className="app-topbar"><button className="menu-button" onClick={()=>setOpen(v=>!v)} aria-label="Abrir menu">☰</button><div><p className="eyebrow">PAINEL DE CONTROLE</p><h1>{navigation.find(([key])=>key===page)?.[1]||'Visão geral'}</h1></div><span className="user-pill">{user?.name||user?.email||'Conta'}</span></header>{children}</div></div> }
